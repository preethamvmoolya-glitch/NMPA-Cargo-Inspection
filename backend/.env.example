# MongoDB Connection URI (Atlas Cluster or local)
# Replace <username>, <password>, and <cluster-url> with your connection details
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/nmpa_cargo?retryWrites=true&w=majority

# Flask configuration
PORT=5000
FLASK_ENV=development
